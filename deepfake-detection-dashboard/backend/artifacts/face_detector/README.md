# OpenCV face detector

Run `python scripts/download_face_detector.py` to obtain:

- `deploy.prototxt`
- `res10_300x300_ssd_iter_140000.caffemodel`

The application does not download model files during startup.
